name: Run Shell Commands
on: [push]

jobs:
  run-shell-command:
    runs-on: windows-latest
    steps:
      - name: Echo a String
        run: echo "Hello World, This is our first GitHub Actions Workflow file"